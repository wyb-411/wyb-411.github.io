# Raincat-wiki Agent Instructions

- Treat `PRD 13.46.01.md` as the canonical product source of truth. For any planning, implementation, UI, or content decision, read it carefully and preserve its constraints instead of summarizing them away.
- For any website, visual frontend, UI, interactive prototype, animation, dashboard, or page-design work, load and strictly follow `.agents/skills/web-design-engineer/SKILL.md`.
- Follow the skill's mandatory workflow for design work: verify uncertain current facts first, gather design context, answer the positioning questions, declare the design system before coding, show an early v0 for direction checks, then complete the build and run the pre-delivery verification checklist.
- Use the skill's reference files only on demand, following its `References Routing` table. Do not bulk-load unrelated reference material.
- Raincat-wiki's PRD design system is product-specific and has priority for this project: Chinese personal blog, repository Markdown/JSON content, dashboard-first entry, glassmorphism cards, dynamic soft gradient backgrounds, large rounded corners, gentle healing tone, and the listed page/widget requirements. If a generic skill rule conflicts with an explicit PRD requirement, satisfy the PRD while preserving the skill's craft, validation, and anti-fabrication discipline.
- Treat `https://lvyovo-wiki.tech/` as the primary product/design reference for future ambiguous choices: repo-file content model, right-top settings modal, independent write page, English dynamic greeting, and soft healing glassmorphism are the default answers unless the user explicitly overrides them.
- Do not add a `/settings` route. Settings live in `src/components/settings/SettingsModal.tsx` and open from the right-top button.
- Content source of truth is the repo: `public/site/*.json`, `public/blogs/index.json`, `public/blogs/categories.json`, `public/blogs/<slug>/config.json`, and `public/blogs/<slug>/index.md`. Admin writes should commit those files through a GitHub App `.pem` flow.
